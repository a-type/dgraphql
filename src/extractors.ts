import { SelectionNode } from 'graphql';
import { GenericArgs } from './types';

export const extractSelectionFieldName = (
  selectionNode: SelectionNode,
): string => 'TODO';

export const extractSelectionTypeName = (
  selectionNode: SelectionNode,
): string => 'TODO';

export const extractSelectionArguments = (
  selectionNode: SelectionNode,
): GenericArgs => ({});
